// the destructuring assignment systax is a js expression that makes it possible to unpack values
// from arrays, or properties from objects, into distinct variables.

let arr = ['yellow','red','orange','black','purple'];

let [y,r,o,b,p,e] = arr;

// console.log(y);


// console.log(arr[2]);

console.log(y,r,o,b,p,e);


